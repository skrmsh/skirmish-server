tags:
  - user
security:
  - AccessTokenHeader: []
responses:
  200:
    description: Data about the authenticated user.
    schema:
      id: UserInfo
      required:
        - username
        - email
      properties:
        username:
          type: string
        email:
          type: string
